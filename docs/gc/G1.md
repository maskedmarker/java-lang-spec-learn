

```text
将heap划分为多个region区
region开始无标签
新生代分配的多,就标记为Eden区,
经过一次gc后,转移到新的region区,该region被标记为Survivor区
后续再gc后, 对象转移到的region被称为Old区

经过回收的region重新被标记为空白区
```

```text
为每个region创建一个2个bitmap,叫做next/prev
prev记录的是上一次标记的位图
next记录的是本次标记的位图


标记开始 通过SATB（Snapshot At The Beginning）
标记开始后,新生成的对象被当作"活对象",不会被gc掉. 这样肯定会有误判,导致大部分"活对象"的gc会被延后.

标记过程中,如果
```